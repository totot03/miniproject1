# PRD — 약국별 일반의약품 최저가 추천 서비스 (가칭: **약값알림**)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.4 (상태관리 확정) |
| 작성일 | 2026-09-15 |
| 프로젝트 유형 | 미니 프로젝트 (성준팀) |
| 개발 기간 | 1~2주 |
| 진행 방식 | 팀 프로젝트지만 **구현은 1인 단위** — 각자 자기 Claude Code로 전체를 구현 |
| 관련 문서 | [DATABASE.md](./DATABASE.md) · [API.md](./API.md) · [ROADMAP.md](./ROADMAP.md) |

---

## 1. 배경 및 문제 정의

### 1.1 문제

전문의약품(처방약)은 건강보험 요양급여 수가로 가격이 규제되어 약국 간 차이가 거의 없다. 반면 **일반의약품(OTC)** 은 약국이 자율적으로 가격을 정하므로 같은 제품이라도 약국마다 판매가가 다르다. 동일한 타이레놀 500mg 8정이 A약국 2,500원, B약국 4,000원인 상황이 실제로 발생한다.

그러나 소비자 입장에서는:

- 약국별 OTC 가격을 사전에 알 수 있는 공개 채널이 사실상 없다.
- 비교하려면 직접 여러 약국을 방문하거나 전화해야 한다.
- 결과적으로 "가장 가까운 약국"에서 가격 확인 없이 구매하게 된다.

### 1.2 해결 가설

**크라우드소싱**으로 약국별 OTC 실거래가를 모으고, 사용자 위치 기준으로 통계 처리한 대표가격을 비교해주면, 사용자는 "근처에서 이 약을 가장 싸게 파는 약국"을 몇 초 만에 알 수 있다.

### 1.3 이 프로젝트의 범위

미니 프로젝트이므로 **실제 사용자 제보를 모으는 것이 목표가 아니다.** 목표는 다음 세 가지다.

1. 크라우드소싱 데이터 모델과 통계 기반 추천 로직이 **동작하는 형태로 구현**되어 있을 것
2. 데이터는 대부분 **목데이터(시드)** 로 채우되, 스키마와 API는 실제 제보가 유입되어도 그대로 확장 가능할 것
3. 위치 기반 검색 → 가격 비교 → 추천까지의 **핵심 사용자 흐름이 끊김 없이 데모 가능**할 것

---

## 2. 목표 / 비목표

### 2.1 목표 (Goals)

| # | 목표 | 성공 판정 |
|---|---|---|
| G1 | 일반의약품에 한해 약국별·지역별 가격 데이터를 축적하는 DB를 구축한다 | 약국 ≥ 300개, 의약품 ≥ 30종, 가격 제보 ≥ 3,000건이 시드로 적재되고 조회된다 |
| G2 | 사용자가 약국명·약품명·가격을 입력해 데이터를 제보할 수 있다 | 폼 제출 → DB 저장 → 즉시 검색 결과에 반영되는 흐름이 동작한다 |
| G3 | 사용자 위치 기준으로 통계 기반 최저가 추천을 제공한다 | 좌표 + 약품 입력 시 반경 내 약국이 Score 순으로 정렬되어 반환된다 |
| G4 | (선택) 관리자가 지역별·약품별 가격 통계를 볼 수 있다 | 관리자 페이지에서 지역별 평균가·최저가·제보 수 차트가 렌더링된다 |

### 2.2 비목표 (Non-Goals)

이번 범위에서 **하지 않는 것**을 명시한다. (구현 중 스코프 확장 방지용)

- 전문의약품(처방약) 가격 — 가격 규제 대상이라 비교 의미가 없음
- 영수증 이미지 OCR 자동 인식 — 영수증은 **첨부 파일 보관 + 관리자 참고용**으로만 취급
- 실제 결제, 약 예약/주문, 재고 확인
- 약국 사업자 계정 / 약국이 직접 가격을 등록하는 기능
- 소셜 로그인(카카오·구글), 비밀번호 찾기, 이메일 인증 메일 발송
- access 토큰 즉시 무효화(블랙리스트) — 로그아웃은 refresh 토큰 revoke까지만 한다
- 제보 스팸 방지를 위한 rate limiting — 중복 제보 차단(F2-8)으로 대체한다
- 모바일 네이티브 앱 (반응형 웹으로 대체)
- 실시간 알림(푸시), 즐겨찾기, 가격 변동 알림
- 프로덕션 클라우드 배포, 컨테이너화 — **각자 로컬에서 직접 실행하는 것까지만** 다룬다 (Docker 미사용)

---

## 3. 사용자 및 시나리오

### 3.1 페르소나

**P1. 절약형 소비자 — 김민지 (28세, 직장인)**
- 상비약을 미리 사두는 편. 편의점보다 약국이 싸다는 건 알지만 어느 약국이 싼지는 모름.
- 니즈: "지금 내 위치에서 걸어갈 만한 거리 중에 타이레놀 제일 싼 데 어디?"

**P2. 제보자 — 박준호 (34세, 커뮤니티 활동 활발)**
- 약을 사고 영수증을 받으면 가격을 공유하는 데 거부감이 없음.
- 니즈: "30초 안에 끝나는 입력 폼". 입력이 길면 이탈한다.

**P3. 운영자 — 팀 관리자**
- 니즈: 데이터가 제대로 쌓이고 있는지, 이상한 가격 제보는 없는지 한눈에 보고 싶다.

### 3.2 핵심 사용자 흐름 (Happy Path)

```
[검색 흐름]
홈 진입
  → 위치 권한 허용 (거부 시 지역 직접 선택으로 폴백)
  → 약품 검색창에 "타이레놀" 입력 → 자동완성에서 품목 선택
  → 반경 선택 (기본 2km)
  → 결과: 지도에 약국 마커 + 하단 리스트(Score 순)
  → 리스트 항목 탭 → 약국 상세 (가격 이력, 제보 수, 최근 갱신일, 전화/길찾기)

[제보 흐름]
약국 상세 또는 상단 "가격 제보하기"
  → 약국 선택(검색/지도에서) → 약품 선택 → 가격 입력 → (선택)구매일, 영수증 첨부
  → 제출 → "제보 감사합니다" → 해당 약국·약품 통계 즉시 재계산

[관리자 흐름]
/admin 로그인(ADMIN 권한)
  → 대시보드: 전체 제보 수, 최근 7일 추이, 커버리지
  → 지역별 통계: 시/군/구 × 약품별 평균가·최저가·최고가
  → 제보 관리: 이상치 플래그된 제보 목록 → 숨김/복구 처리
```

---

## 4. 기술 스택 및 주요 결정

| 레이어 | 선택 | 비고 |
|---|---|---|
| 프론트엔드 | Next.js 16 (App Router) + React 19.3 + TypeScript strict | 서버 컴포넌트로 검색 결과 SSR, 지도/폼은 클라이언트 컴포넌트 |
| 스타일 | Tailwind CSS + shadcn/ui | 컴포넌트 직접 작성 시간 절약 |
| 상태 관리 | TanStack Query(서버 상태) + Redux Toolkit(클라이언트 상태) | 역할 경계는 §4.2 참고. 초기 렌더는 서버 컴포넌트, URL에 남아야 할 상태는 쿼리 파라미터 |
| 폼 | React Hook Form + Zod | 제보 폼·인증 폼 검증 |
| 지도 | Kakao Maps JavaScript SDK | 국내 약국 좌표 정확도 및 한글 주소 표기 |
| 위치 취득 | 브라우저 `navigator.geolocation` | 권한 거부/실패 시 시·군·구 드롭다운으로 폴백 |
| 백엔드 | Spring Boot 4.1.x + Java 21 | Spring Boot 4의 Java 최소 요구는 17이지만 21(LTS)을 쓴다 |
| DB | PostgreSQL 17 | 좌표는 `double precision` 컬럼 + 바운딩 박스 선필터 + Haversine 계산 |
| 타임존 | 서버·DB 모두 `Asia/Seoul` | 날짜 경계가 사용자 체감과 일치해야 중복 제보 방지·날짜 검증이 맞는다 ([DATABASE.md](./DATABASE.md) §8) |
| 인증 | 이메일/비밀번호 + JWT (Access 30분 / Refresh 14일, 회전) | 역할: `USER`, `ADMIN`. refresh 토큰 해시는 DB 보관, 로그아웃 시 revoke |
| 파일 저장 | 로컬 볼륨 (`/app/uploads`) | 영수증 이미지. S3는 향후 확장 지점으로만 설계 |
| 빌드 | **Maven** (Maven Wrapper `mvnw` 커밋) | Spring Boot 4.1은 Maven 3.6.3 이상 필요 |
| 마이그레이션 | Flyway | `V1__init.sql`, `V2__seed_master.sql`, `V3__seed_prices.sql` |
| 실행 | 로컬 직접 실행 (**Docker 미사용**) | PostgreSQL은 로컬 설치, 백엔드 `./mvnw spring-boot:run`, 프론트 `npm run dev` |
| 테스트 | JUnit 5 + Testcontainers (Score 로직 중심), Vitest (FE 유틸) | 전면 커버리지 아님, 핵심 로직만 |

### 4.1 프로젝트 구조

```
miniProject1/
├─ PRD.md · DATABASE.md · API.md · ROADMAP.md · README.md
├─ .env.example · .editorconfig · .gitignore
├─ miniproject1-frontend/     Next.js 16 + React 19.3
├─ miniproject1-backend/      Spring Boot 4.1 + Java 21 + Maven
└─ tools/seed-generator/      공공데이터 → 시드 SQL 변환 (Python)
```

문서 4종은 루트에 평평하게 둔다. 자세한 규칙은 [ROADMAP.md](./ROADMAP.md) T-01 참고.

### 4.2 프론트 상태 관리 경계

도구가 넷이라 겹치기 쉽다. 아래 기준으로 나눈다.

| 상태 종류 | 도구 | 예 |
|---|---|---|
| 초기 렌더에 필요한 서버 데이터 | **서버 컴포넌트에서 직접 fetch** | 검색 결과(`/search`), 약국 상세 |
| 클라이언트에서 다시 불러오는 서버 데이터 | **TanStack Query** | 약품 자동완성, 가격 이력, 제보 후 갱신, 관리자 통계 |
| 화면 간 공유되는 클라이언트 상태 | **Redux Toolkit** | 사용자 위치(`locationSlice`), 인증 세션(`authSlice`), 제보 폼 임시 입력(`reportDraftSlice`) |
| URL에 남아야 하는 상태 | **URL 쿼리** | `drugId`, `lat`, `lng`, `radius`, `sort` — 공유·뒤로가기 동작 |
| 한 컴포넌트 안에서만 쓰는 상태 | `useState` | 드롭다운 열림, 아코디언 펼침 |

> ⚠️ 검색 결과를 TanStack Query로 옮기지 않는다. 서버 컴포넌트 SSR + URL 쿼리가 이미 그 역할을 한다. 둘 다 쓰면 데이터가 두 곳에 생긴다.

### 4.3 결정 근거 메모

- **PostGIS를 쓰지 않는다.** 반경 2~5km 내 수백 건 규모라 확장 설치·학습 비용 대비 이득이 없다. `lat BETWEEN ? AND ? AND lng BETWEEN ? AND ?` 로 후보를 좁힌 뒤 Haversine으로 정확 거리를 구한다. 데이터가 커지면 `cube` + `earthdistance` 또는 PostGIS로 교체 가능하도록 거리 계산을 별도 컴포넌트로 분리한다.
- **통계 집계는 캐시 테이블에 저장한다.** 매 검색마다 전체 제보를 집계하면 느리고 이상치 제거 로직이 중복된다. `pharmacy_drug_price_stat` 테이블에 대표가격을 미리 계산해 두고, 제보 저장 시 해당 (약국, 약품) 조합만 재계산한다.

---

## 5. 데이터 소스 전략

| 데이터 | 출처 | 방식 |
|---|---|---|
| 약국 목록 (이름, 주소, 좌표, 전화) | 국립중앙의료원 전국 약국 정보 조회 서비스 / 심평원 전국 병의원·약국 현황 | 개발 초기에 **1회 내려받아 CSV → 시드 SQL로 변환**. 런타임 API 호출 없음 |
| 의약품 마스터 (품목명, 제조사, 효능, 낱알 이미지) | 식약처 의약품개요정보(e약은요) | 일반의약품 30~50종만 추려서 시드 |
| 행정구역 코드 (시도/시군구) | 행정안전부 표준 코드 | 정적 시드 |
| **가격 데이터** | **없음 → 전량 생성** | 약품별 기준가에 약국 특성(지역·규모) 기반 ±20% 변동 + 정규분포 노이즈를 적용해 생성. 일부는 의도적 이상치로 삽입해 IQR 필터 동작을 데모 |

가격 목데이터 생성 규칙 (시드 스크립트):

- 약품마다 `base_price` 를 정의 (예: 타이레놀정 500mg 8정 = 2,800원)
- 약국마다 `price_factor` 를 0.85 ~ 1.25 범위에서 부여 (약국 고유 가격대 성향)
- 제보 1건 = `round(base_price × price_factor × N(1, 0.05), -1)` (10원 단위 반올림)
- 약국·약품 조합당 1~6건, `purchased_at` 은 최근 120일에 분포
- 전체의 약 2%는 `base_price × (0.3 or 3.0)` 로 생성 → 오타 제보 시뮬레이션

> ⚠️ 이 데이터는 **실제 판매가가 아님**을 UI 상단 배너와 푸터에 상시 고지한다. (§9 참고)

---

## 6. 기능 요구사항

우선순위 표기: **P0** = 없으면 데모 불가 / **P1** = 있어야 완성도 / **P2** = 시간 남으면

### F1. DB 구축 — 일반의약품 가격 데이터 축적 (P0)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F1-1 | 약국 마스터를 저장한다: 이름, 도로명/지번 주소, 시도·시군구 코드, 위도, 경도, 전화번호, 영업시간(JSON) | P0 |
| F1-2 | 일반의약품 마스터를 저장한다: 품목기준코드, 품목명, 제조사, 분류(해열진통/소화제/감기약 등), 제형, 포장단위, 이미지 URL | P0 |
| F1-3 | 가격 제보를 저장한다: 약국, 약품, 가격, 구매일, 제보자, 입력 경로(`FORM`/`SEED`), 상태(`ACTIVE`/`HIDDEN`), 영수증 파일 참조 | P0 |
| F1-4 | 약국 × 약품 조합별 통계를 캐시 테이블에 유지한다: 대표가격(=중앙값), 최저가, 최고가, 평균가, 제보 수, 최근 제보일 | P0 |
| F1-5 | 제보가 추가/숨김 처리될 때 해당 조합의 통계가 자동 재계산된다 | P0 |
| F1-6 | 전문의약품은 저장 대상에서 제외한다 (`drug.otc_flag = true` 인 것만 적재·노출) | P0 |
| F1-7 | 시드 데이터로 약국 ≥ 300건, 약품 ≥ 30종, 제보 ≥ 3,000건이 적재된다 | P0 |
| F1-8 | 약국 검색을 위해 이름·주소에 대한 부분 일치 검색이 가능하다 | P1 |

**수용 기준**
- 백엔드 기동 시 Flyway 마이그레이션이 자동 실행되어 시드 데이터가 들어간다.
- `GET /api/v1/drugs?q=타이레놀` 이 200과 함께 1건 이상을 반환한다.
- 전문의약품 코드로 조회 시 결과에 포함되지 않는다.

---

### F2. 가격 입력 — 사용자 제보 (P0)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F2-1 | 로그인한 사용자는 약국·약품·가격을 폼으로 제출할 수 있다 | P0 |
| F2-2 | 약국 선택은 (a) 이름 검색 자동완성 (b) 지도에서 마커 선택 두 가지를 지원한다 | P0 |
| F2-3 | 약품 선택은 품목명 자동완성으로 하며, 마스터에 없는 약품은 선택할 수 없다 | P0 |
| F2-4 | 가격은 100원 이상 200,000원 이하의 정수만 허용한다 | P0 |
| F2-5 | 구매일은 선택 입력이며, 미입력 시 제출일로 간주한다. 미래 날짜와 180일 초과 과거는 거부한다 | P0 |
| F2-6 | 영수증 이미지를 1장 첨부할 수 있다 (jpg/png/webp, 5MB 이하). **필수 아님, OCR 없음** | P1 |
| F2-7 | 제출 성공 시 해당 (약국, 약품) 통계가 재계산되고, 사용자는 갱신된 약국 상세로 이동한다 | P0 |
| F2-8 | 같은 사용자가 같은 (약국, 약품)에 대해 같은 날(KST) 중복 제보하면 거부한다 | P1 |
| F2-9 | 입력된 가격이 해당 약품 전체 중앙값의 0.3배 미만 또는 3배 초과이면 저장은 하되 `flagged = true` 로 표시하고, 통계 계산에서 제외한다 | P1 |
| F2-10 | 비로그인 사용자가 제보 버튼을 누르면 로그인 페이지로 유도한다 | P0 |

**수용 기준**
- 가격에 `0`, `-100`, `abc` 입력 시 클라이언트·서버 양쪽에서 검증 실패 메시지가 뜬다.
- 제보 직후 같은 약품으로 검색하면 해당 약국의 대표가격이 반영되어 있다.
- 이상치 가격(예: 평균 3,000원 약품에 50,000원)을 넣으면 목록에는 보이되 대표가격에는 영향을 주지 않는다.

---

### F3. 최저가 추천 (Score) (P0)

#### F3.1 대표가격 산출

특정 (약국, 약품) 조합의 대표가격 `P_rep` 은 다음 절차로 계산한다.

1. 대상 제보: `status = ACTIVE` AND `flagged = false` AND `purchased_at >= CURRENT_DATE - 90일`
2. 90일 내 제보가 0건이면 → 창을 180일로 확대해 재시도. 그래도 0건이면 **해당 약국은 추천 후보에서 제외**
3. 제보가 4건 이상이면 IQR 이상치 제거: `Q1 - 1.5×IQR` 미만 또는 `Q3 + 1.5×IQR` 초과 값 제외
4. 남은 값의 **중앙값(median)** 을 `P_rep` 으로 한다
5. `P_min`, `P_avg`, `report_count`, `last_reported_at` 을 함께 저장한다

> 평균이 아니라 중앙값을 쓰는 이유: 제보 수가 적을 때(3~5건) 오타 한 건이 평균을 크게 흔들기 때문이다. 평균은 화면 표시용 참고값으로만 쓴다.

#### F3.2 추천 Score

사용자 위치 `(lat_u, lng_u)`, 약품 `D`, 반경 `R`(기본 2,000m)이 주어지면:

**후보군**: 반경 `R` 내 약국 중 약품 `D` 의 `P_rep` 이 존재하는 약국

**각 후보 i에 대해:**

```
PriceScore_i     = (P_max - P_rep_i) / (P_max - P_min)      # 후보군 내 min-max 정규화
                   단, P_max == P_min 이면 1.0

DistanceScore_i  = clamp(1 - (d_i / R), 0, 1)               # d_i = Haversine 거리(m)

FreshnessScore_i = 0.5 ^ (age_days_i / 30)                  # 반감기 30일
                   age_days_i = now() - last_reported_at_i

Score_i = 0.60 × PriceScore_i
        + 0.25 × DistanceScore_i
        + 0.15 × FreshnessScore_i
```

- `P_max`, `P_min` 은 **후보군 내** 최대/최소 대표가격
- 가중치 `0.60 / 0.25 / 0.15` 는 `application.yml` 에서 조정 가능하게 둔다 (`recommendation.weights.*`)
- 최종 결과는 `Score` 내림차순 정렬, 동점이면 `P_rep` 오름차순 → 거리 오름차순

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F3-1 | 브라우저 Geolocation으로 좌표를 받아 검색한다. 권한 거부 시 시·군·구 선택으로 폴백하고 해당 구역 중심 좌표를 사용한다 | P0 |
| F3-2 | 반경은 500m / 1km / 2km / 5km 중 선택, 기본 2km | P0 |
| F3-3 | 결과는 Score 순 정렬이 기본이며, 사용자가 "가격순" / "거리순"으로 전환할 수 있다 | P0 |
| F3-4 | 1위 약국은 "최저가 추천" 배지와 함께 강조 표시한다 | P0 |
| F3-5 | 각 항목에 대표가격, 최저가, 거리, 제보 수, 최근 갱신일(예: "3일 전")을 표시한다 | P0 |
| F3-6 | 후보군 평균가 대비 절약액을 표시한다 (예: "평균보다 700원 저렴") | P1 |
| F3-7 | 카카오맵에 후보 약국을 마커로 표시하고, 리스트 항목과 마커가 상호 연동된다 | P1 |
| F3-8 | 제보 수 1건 또는 30일 초과 경과 데이터는 "정보 부족" / "오래된 정보" 뱃지를 표시한다 | P1 |
| F3-9 | 반경 내 결과가 0건이면 반경 확대를 제안한다 | P1 |

**수용 기준**
- 같은 약품·같은 좌표로 두 번 호출하면 동일한 순위가 나온다 (결정적).
- 가장 싼 약국이 반경 경계에 있고 두 번째로 싼 약국이 바로 앞에 있으면, 거리 가중치로 인해 순위가 뒤집힐 수 있으며 이는 의도된 동작이다 (테스트로 고정).
- 제보가 없는 약국은 결과에 나타나지 않는다.

---

### F4. 관리자 통계 (P2 — 시간 여유 시)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F4-1 | `ADMIN` 역할만 `/admin` 에 접근할 수 있다 | P2 |
| F4-2 | 대시보드: 총 약국 수, 총 약품 수, 총 제보 수, 최근 7일 제보 추이(라인 차트) | P2 |
| F4-3 | 지역별 통계: 시·군·구 × 약품별 평균가 / 최저가 / 최고가 / 제보 수 테이블 | P2 |
| F4-4 | 약품별 가격 분포: 선택한 약품의 전국 가격 히스토그램 + 지역별 평균 비교 막대 차트 | P2 |
| F4-5 | 이상치 제보 목록: `flagged = true` 인 제보를 모아 보고, 숨김/복구 처리 | P2 |
| F4-6 | 데이터 커버리지: 약품별로 가격 정보가 있는 약국 비율 | P2 |

> **권장 통계 지표** (F4-3 외에 추가하면 좋은 것): 지역 간 가격 격차 순위(같은 약품의 시군구별 평균가 최대-최소 차이가 큰 약품 Top 10). "어느 약이 지역에 따라 가장 편차가 큰가"는 서비스의 존재 이유를 가장 잘 보여주는 지표다.

---

## 7. 화면 정의

| 라우트 | 화면 | 주요 요소 | 우선순위 |
|---|---|---|---|
| `/` | 홈 / 검색 | 약품 검색창(자동완성), 위치 표시 및 재설정, 인기 약품 칩, 서비스 설명 | P0 |
| `/search?drugId=&lat=&lng=&radius=` | 검색 결과 | 상단 지도(카카오맵) + 하단 약국 리스트, 정렬 토글, 반경 필터 | P0 |
| `/pharmacies/{id}` | 약국 상세 | 약국 정보(주소·전화·영업시간), 취급 약품별 대표가격 표, 가격 이력 스파크라인, "이 약국에 가격 제보" 버튼 | P0 |
| `/reports/new` | 가격 제보 | 약국 선택 → 약품 선택 → 가격 → 구매일 → 영수증 첨부 (4스텝 or 단일 폼) | P0 |
| `/login`, `/signup` | 인증 | 이메일·비밀번호 | P0 |
| `/me` | 내 제보 | 내가 올린 제보 목록, 상태 표시 | P1 |
| `/admin` | 관리자 대시보드 | 요약 지표 + 추이 차트 | P2 |
| `/admin/stats` | 관리자 통계 | 지역별·약품별 통계 테이블·차트 | P2 |
| `/admin/reports` | 제보 관리 | 이상치 목록, 숨김/복구 | P2 |

**반응형**: 모바일 우선. 모바일에서는 지도가 상단 40vh, 리스트가 바텀시트 형태로 올라오는 구조를 기본으로 한다.

---

## 8. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 성능 | 반경 2km 검색 응답 p95 < 500ms (시드 데이터 규모 기준). 자동완성 응답 < 200ms |
| 확장성 | 가격 제보가 실사용자로부터 유입되어도 스키마 변경 없이 수용 가능해야 한다 (`source` 컬럼으로 `SEED`/`FORM` 구분) |
| 보안 | 비밀번호는 BCrypt 해시. JWT는 서명 검증. 관리자 API는 `@PreAuthorize("hasRole('ADMIN')")` 로 보호 |
| 입력 검증 | 모든 쓰기 API는 Bean Validation 적용. 클라이언트 검증은 UX용일 뿐 신뢰하지 않는다 |
| 접근성 | 폼 라벨·에러 메시지 연결, 키보드 내비게이션, 색상만으로 정보 전달하지 않기 |
| 국제화 | 한국어 단일. 통화는 KRW 고정. 서버·DB 타임존은 `Asia/Seoul` 고정 |
| 브라우저 | 최신 Chrome / Safari / Edge. IE 미지원 |
| 로깅 | 요청별 traceId, Score 계산 결과는 DEBUG 레벨로 기록 (순위 근거 추적용) |
| 실행 | README 절차만 따라 로컬 PostgreSQL 준비 후 백엔드·프론트를 각각 기동하면 시드까지 적재 완료 |

---

## 9. 리스크 및 법적·윤리적 고려사항

| 리스크 | 영향 | 대응 |
|---|---|---|
| **목데이터가 실제 가격으로 오인됨** | 사용자가 잘못된 가격 정보로 약국을 방문 | 모든 화면 상단에 "본 서비스의 가격은 학습용 예시 데이터입니다" 고정 배너. 푸터에도 명시. 시드 데이터임을 API 응답에도 `dataSource: "SEED"` 로 노출 |
| 약국의 명예·영업 침해 소지 | 실명 약국에 가짜 가격이 붙는 문제 | 시드 약국명을 실제 공공데이터로 쓰되, **README와 UI에 데모 목적임을 명시**. 대외 공개·배포하지 않음 (로컬 데모 한정) |
| 의약품 광고·판매 규제 | 약사법상 일반의약품 가격 비교 서비스는 회색지대 | 미니 프로젝트·비공개 데모 범위로 한정. 실서비스화 시 법률 검토 필요함을 문서에 남긴다 |
| 크라우드소싱 데이터 신뢰도 | 오타·악의적 제보가 순위를 왜곡 | IQR 이상치 제거 + flagged 처리 + 관리자 숨김 기능 (F2-9, F4-5) |
| 카카오맵 API 키 노출 | 클라이언트 JS SDK 특성상 키가 노출됨 | 카카오 콘솔에서 허용 도메인을 `localhost` 로 제한. 키는 `.env.local` 로 관리하고 커밋하지 않음 |
| 위치 권한 거부 | 핵심 흐름 진입 불가 | 시·군·구 선택 폴백을 P0로 지정 (F3-1) |
| 1~2주 일정 초과 | 데모 불가 | 관리자 통계(F4)를 P2로 명확히 분리. 스프린트 1 종료 시 P0 완료를 게이트로 둔다 ([ROADMAP.md](./ROADMAP.md) 참고) |

---

## 10. 성공 지표 (데모 기준)

미니 프로젝트이므로 실사용 지표가 아닌 **데모 완결성**으로 측정한다.

| 지표 | 목표 |
|---|---|
| 핵심 흐름 무중단 데모 | 검색 → 결과 → 상세 → 제보 → 재검색까지 에러 없이 완주 |
| 시드 데이터 규모 | 약국 ≥ 300, 약품 ≥ 30, 제보 ≥ 3,000 |
| Score 로직 단위 테스트 | 가격/거리/신선도 각 요인이 순위에 미치는 영향을 검증하는 테스트 ≥ 6개 통과 |
| 검색 응답 시간 | p95 < 500ms |
| 이상치 제거 데모 | 의도적 오타 제보가 대표가격에 반영되지 않음을 화면으로 보여줄 수 있음 |
| 재현 가능한 기동 | 클린 클론 상태에서 README 절차만으로 동작 |

---

## 11. 향후 확장 (이번 범위 밖)

이번에 만들지는 않지만 **설계상 막히지 않도록** 고려해 둔 지점들이다.

- **실사용자 제보 유입**: `price_report.source` 에 `FORM`/`SEED` 외 `RECEIPT_OCR`, `PARTNER` 추가 가능
- **제보자 신뢰도**: `user` 에 `trust_score` 를 추가해 가중 평균으로 대표가격 계산
- **영수증 OCR**: `price_report.receipt_file_id` 를 이미 갖고 있으므로 비동기 워커만 붙이면 됨
- **약국 사업자 계정**: `pharmacy.owner_user_id` 추가 → 약국이 직접 가격 갱신
- **가격 변동 알림**: `pharmacy_drug_price_stat` 변경 이력을 이벤트로 발행
- **PostGIS 전환**: 거리 계산이 `DistanceCalculator` 인터페이스로 분리되어 있어 구현체만 교체
- **컨테이너화 및 클라우드 배포**: Docker Compose로 로컬 통합 → Vercel(FE) + 컨테이너 런타임(BE) + 관리형 PostgreSQL

---

## 12. 열린 질문

구현 착수 전 팀 내에서 확정이 필요한 항목.

| # | 질문 | 기본값(미정 시) |
|---|---|---|
| Q1 | 서비스명을 무엇으로 할 것인가 | "약값알림" (가칭) |
| Q2 | 시드 대상 지역을 전국으로 할 것인가, 특정 지역(예: 서울)으로 좁힐 것인가 | 서울·경기 일부로 좁혀 밀도를 확보 |
| Q3 | Score 가중치 0.60/0.25/0.15를 그대로 갈 것인가 | 그대로 시작하고 데모 데이터로 튜닝 |
| Q4 | 관리자 계정을 시드로 박을 것인가, 별도 가입 후 권한 부여할 것인가 | 시드로 `admin@example.com` 생성 |
| Q5 | 영수증 첨부(F2-6)를 P1으로 유지할 것인가, 잘라낼 것인가 | 유지하되 스프린트 2 후반 배치 |

---

*이 문서는 요구사항 정의를 담당한다. 테이블 설계는 [DATABASE.md](./DATABASE.md), 엔드포인트 명세는 [API.md](./API.md), 태스크 분해와 진행 순서는 [ROADMAP.md](./ROADMAP.md)를 참고할 것.*
